# TimeTree認証情報（ローカル実行用の参考値。GitHub上では Secrets を使用）
TIMETREE_EMAIL=your-email@example.com
TIMETREE_PASSWORD=your-password
TIMETREE_CALENDAR_CODE=your-calendar-code

# 通知方式（どちらかを使用）
# 1) LINE Notify を使う場合
LINE_TOKEN=your-line-notify-token

# 2) LINE Messaging API を使う場合
LINE_CHANNEL_ACCESS_TOKEN=your-channel-access-token
LINE_USER_ID=your-user-id

# 注意:
# - このファイルはサンプルです。実際の秘密情報は絶対にコミットしないでください。
# - リポジトリでは GitHub Secrets（Settings → Secrets and variables → Actions）に保存してください。
